import "./contrastPreview.css";
import React, { useState, useEffect } from "react";

const ContrastPreview = ({title}) => {
  const [] = useState();
  useEffect(() => {},[]);

  return (
    <div className="contrastPreview">
       <h1>{title}</h1>
    </div>
  );
};

export default ContrastPreview;
